const esc=s=>String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
let jobs=[];const list=document.querySelector('#job-list'),search=document.querySelector('#search'),location=document.querySelector('#location'),team=document.querySelector('#team');
function card(j){return `<article class="job-card"><div><span class="job-team">${esc(j.team)}</span><h3>${esc(j.title)}</h3><p>${esc(j.summary)}</p><div class="job-meta"><span>⌖ ${esc(j.location)}</span><span>◷ ${esc(j.type)}</span><span>↗ ${esc(j.experience)}</span></div></div><a href="job.html?id=${encodeURIComponent(j.id)}" aria-label="View ${esc(j.title)}">→</a></article>`}
function render(){let found=jobs.filter(j=>[j.title,j.team,j.location,j.summary].join(' ').toLowerCase().includes(search.value.toLowerCase())&&(!location.value||j.location===location.value)&&(!team.value||j.team===team.value));list.innerHTML=found.length?found.map(card).join(''):`<div class="empty">No roles match those filters. Try another search.</div>`;document.querySelector('#results').textContent=`${found.length} open role${found.length===1?'':'s'}`}
fetch('/api/jobs').then(r=>r.json()).then(data=>{jobs=data;document.querySelector('#role-count').textContent=data.length;for(const prop of ['location','team']) [...new Set(data.map(j=>j[prop]))].sort().forEach(v=>document.querySelector('#'+prop).insertAdjacentHTML('beforeend',`<option>${esc(v)}</option>`));render()});[search,location,team].forEach(el=>el.addEventListener('input',render));document.querySelector('#reset').onclick=()=>{search.value=location.value=team.value='';render()};
