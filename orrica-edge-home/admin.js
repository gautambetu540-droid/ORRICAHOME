const form=document.querySelector('#job-form'), status=document.querySelector('#form-status');form.addEventListener('submit',async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));data.skills=data.skills.split(',').map(s=>s.trim()).filter(Boolean);status.textContent='Publishing…';const r=await fetch('/api/jobs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(r.ok){status.textContent='Role published. It is now live on the homepage and Jobs page.';form.reset()}else status.textContent='Could not publish. Please try again.'});
